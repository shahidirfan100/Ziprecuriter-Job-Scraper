{
  "title": "ZipRecruiter Jobs Scraper (Fast & Optimized)",
  "description": "A fast and lightweight actor to scrape job listings from ZipRecruiter using HTTP and Cheerio. Optimized for speed with parallel processing.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL (optional)",
      "type": "string",
      "description": "A specific ZipRecruiter URL to start scraping from. If provided, it overrides Keyword, Location, and Date Posted filters.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword",
      "type": "string",
      "description": "Search term(s) for jobs (e.g., 'Software Developer', 'React', 'Data Analyst').",
      "editor": "textfield",
      "default": "developer",
      "prefill": "developer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location to search for jobs (e.g. 'San Francisco, CA', 'New York'). Defaults to 'United States' to prevent geographic redirects.",
      "default": "United States",
      "editor": "textfield"
    },
    "postedWithin": {
      "title": "Date Posted",
      "type": "string",
      "description": "Filter by how recently the job was posted. Note: This filter only works when using Keyword search (not with custom Start URL).",
      "editor": "select",
      "enum": [
        "any",
        "24h",
        "7d",
        "30d"
      ],
      "enumTitles": [
        "Any time",
        "Last 24 hours",
        "Last 7 days",
        "Last 30 days"
      ],
      "default": "any"
    },
    "results_wanted": {
      "title": "Results Wanted",
      "type": "integer",
      "description": "The maximum number of job listings to scrape.",
      "editor": "number",
      "default": 100,
      "prefill": 20,
      "minimum": 1,
      "maximum": 10000
    },
    "collect_details": {
      "title": "Collect Job Details",
      "type": "boolean",
      "description": "If enabled, the scraper will visit each job's detail page to extract more information (full description, etc.). Disabling this makes the scrape much faster but returns less data.",
      "editor": "checkbox",
      "default": true
    },
    "preferCandidateSearch": {
      "title": "Prefer '/candidate/search' URLs",
      "type": "boolean",
      "description": "If enabled, the scraper will use the '/candidate/search' URL structure instead of '/Jobs/'. This can sometimes provide different results or better pagination stability.",
      "editor": "checkbox",
      "default": false
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "The maximum number of parallel requests. Higher values = faster scraping but may increase blocking risk. Recommended: 8-15 for details, 15-25 without details.",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 50
    },
    "downloadIntervalMs": {
      "title": "Download Interval (ms)",
      "type": "integer",
      "description": "Milliseconds to wait between requests. Lower values = faster but higher block risk. Default: 200ms with details, 100ms without.",
      "editor": "number",
      "minimum": 0,
      "maximum": 5000,
      "nullable": true
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Select proxies to be used by the crawler. Residential proxies are highly recommended for ZipRecruiter.",
      "editor": "proxy",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ],
        "countryCode": "US"
      }
    }
  },
  "required": []
}